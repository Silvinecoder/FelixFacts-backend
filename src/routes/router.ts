import { Router } from "express";

const router = Router();

// Here is where you add your routers example:
// router.post("/courses", createCourse);
// router.get("/courses/:courseId", getCourseStats);

export default router;
